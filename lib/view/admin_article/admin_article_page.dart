import 'package:flutter/material.dart';

class AdminArticlePage extends StatelessWidget {
  const AdminArticlePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is admin article page'),
    );
  }
}
