import 'package:flutter/material.dart';

class AdminChallengePage extends StatelessWidget {
  const AdminChallengePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is admin challenge page'),
    );
  }
}
