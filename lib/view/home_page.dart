import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Todo: Yuxuan this is Homepage
    return const Scaffold(
      body: Center(child: Text('Home Page')),
    );
  }
}
